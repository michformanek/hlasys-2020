# Parameters
#
parameters:

	# System
	system:
		error:
			email: dev@hkfree.org
			presenter: Front:Error

	# Database
	database:
		driver: pdo_mysql

	# Project
	project:
		rev: "1.0.0"

	# File
	files:
		root: %appDir%/../data/files

	# Mailing
	mailing:
		from: hlasys@localhost
		from_name: Hlasys

	# STMP
	smtp:
		host: localhost
		username: fake
		password: fake
